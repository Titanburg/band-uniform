extends layout

block content
  nav.navbar.navbar-default
    .container-fluid
      .navbar-header
        button.navbar-toggle.collapsed(type='button' data-toggle='collapse' data-target='#band-navbar' aria-expanded='false')
          span.sr-only
          span.icon-bar
          span.icon-bar
          span.icon-bar
        a.navbar-brand(href="#") Band Uniform
      .collapse.navbar-collapse(id='band-navbar')
        ul.nav.navbar-nav.navbar-left
          // Admin Only
          - if(user.admin)
            li
              a(href='/users' Users) Users
            li 
              a(href='/uniform' Uniforms) Uniforms
          // User only
          
          // All
          li
            a(href='/maintenance_request' Maintenance) Maintenance
        ul.nav.navbar-nav.navbar-right
          li
            a(href='/account')= user.email
          li
            a(href='/logout') Logout
  .content(ng-view)
